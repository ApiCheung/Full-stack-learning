var todos = ["buy new turtle"];
var input = prompt("what would you like tod\ do");
while(input != "quit"){
    var input = prompt("what would you like tod\ do");

    if(input === "list"){
        console.log(todos);
    }else if(input === "new"){
        var newTodo = prompt("enter new todo");
        todos.push(newTodo);
    }

}
console.log("ok you quit");
//for each

arr.forEach(someFunction){

}
var colors = ["blue","red","yellow"];
colors.forEach(function(ilovedogs){
    console.log("inside" + ilove thelog);
});
//
splice(index, 10);
//从当前位置之后的10个全部都会被删除
function（color， i）
//第二个位置是array中的index的位置
